# 发布门槛（Release Gates）

> 本文档定义 xflow server/runner 的分层生产就绪门槛、各层达成状态和部署承诺。
> 门槛定义源自 `.claude/specs/2026-07-17-server-production-readiness-design.md`，本文档与其保持一致，用于快速了解当前可宣称的生产能力。

> **2026-07-18 事实纠正**：经生产就绪复核（见
> `.claude/specs/2026-07-18-sdk-server-production-readiness-remediation-design.md`），
> 此前 G0/G1/C2 标记"已完成"与代码事实不符，已回退。状态以本节为准；
> 任一条目恢复"已完成"必须满足该 spec §2 的三层证据（实现 + 测试 + 运行 artifact）。
>
> **2026-07-22 发布门重定义**：「运行 artifact」证据层级由本地 podman 真实 Redis/MySQL artifact（含 SHA 绑定 + 结构化报告 JSON）满足；真实 CI run 的 artifact 签出不再作为 G0 闭合前置条件，降为后续 TODO。G0 据此闭合（本地证据）；G1 仍 ⛔（B3 R3 reconcile 收敛为确定性代码缺口，与 CI 无关）；G2 仍 ⏳（真实 HA/多租户环境验收，与 CI 无关，不受本重定义影响）。
>
> **2026-07-19 验收纠正**：对 HEAD `fdc0206` 的复验确认 G0/G1 仍不通过、G2 仍未完成，
> 并再次证伪 B1“修复完成”、A3“三拓扑完成”和 C2“深层不可变完成”的声明。
> 详细证据见 `.claude/specs/2026-07-19-sdk-server-production-readiness-acceptance.md`；
> 后续任务见 `.claude/plans/2026-07-19-sdk-server-production-readiness-followup.md`。
>
> **2026-07-20 重新验收纠正**：对 HEAD `e54ab4f`（2026-07-19 followup 21 commits 后）的重新验收
> 确认 C2 通过、C1 保持通过（限 MySQL 范围），但 G0/G1 仍不通过。本轮探针定位到**确定性代码缺口**：
> A2 missing node status / current activation 仍 fail-open（outcome=replayed）；A1 failed-startup
> 未关闭 ResourcePool（Close calls=0）；A0 OSKill 场景实为优雅 stop 模拟、HandlerInvocations/DAGAdvances
> 为逻辑估算/常量；A3 仅核心 fixture 三拓扑、HTTP/gRPC/script/OnError/Database 缺 durable SDK cluster；
> B1 submit→dispatch parent graph 断链（pollTask 未注入 tenant 到 ExecutionTraceCarrier，非 default tenant
> 查错 namespace），测试只 WARN 不 fail；B3 workflow create/invoke admission 写入空 ExecutionID、
> Probe 对多数 mutation 固定 Indeterminate、reconciled_by_worker=0 仍通过；CI 仅 Redis required，
> MySQL/Kafka 可 Skip，无 G1/parity/trace 独立 artifact 与 manifest。详细证据见
> `.claude/specs/2026-07-20-sdk-server-production-readiness-reacceptance.md`；
> 后续任务见 `.claude/plans/2026-07-20-sdk-server-production-readiness-followup.md`。
> 本轮（R0）仅下调过度声明为诚实状态，不提前变化总状态。**R1/R2/R4 修复确定性代码缺口后已逐项回写本表（A1/A2/B1/G1-e2e）：代码侧三缺口已闭合。G0 整体状态 2026-07-22 重定义为已完成（本地证据；CI artifact 降为后续 TODO）；G1 仍 ⛔（R3 B3 reconcile 未做；reconcile 弱证据需 R3 升级为强断言）**（~~CI artifact ENV-GATED~~ → CI 签出降为 TODO + R3 B3 reconcile 未做 + ~~R5 真实进程矩阵未做~~ → R5 的 OSKill 真实进程升级降为 TODO + ~~R7 CI required+manifest 未做~~ → R7 降为 TODO + ~~R8 真实 binary e2e 未做~~ → **R8 已闭合 2026-07-22**，见下文 R8 段）。

## 1. 门槛分层

xflow 采用分层发布门槛，不再用单个测试替代完整 release gate。语义约束：handler 与 Runner Protocol 保持 **at-least-once**；生产承诺是不丢调度意图、重复提交不重复推进 DAG，业务副作用由宿主幂等键兜底，**不承诺 handler exactly-once**。

| 门槛 | 适用场景 | 要求 |
|---|---|---|
| **G0 — 调度可靠性候选** | 非生产承诺 | A0～A3：真实 Redis cyclic 恢复、control-plane binder fail-closed、dead-letter 安全处置、跨进程错误语义一致。G0 证明核心调度恢复路径具备候选可靠性，不包含 HA、业务 API 安全或租户承诺。 |
| **G1 — 受限生产（可信单租户）** | 可信网络、单租户、允许控制面维护窗口的复杂审批部署 | G0 + B1（OTel）+ B3（API authz）。workflow/control API 强制认证授权，生产配置不默认匿名；审批工单、权限、不可变审批事件、审计和幂等键由宿主持有；Redis 可恢复部署并完成备份/恢复演练；dead-letter 告警/处置/审计可操作；tracing/metrics/logging 具备值班关联能力。若 control plane 为单副本，明确「不提供无中断 HA SLO」和维护窗口。 |
| **G2 — 多副本 HA / 多租户生产** | 多副本 HA、多租户 | G1 + B2。多租户部署还必须完成 tenant boundary 实现与安全测试。验收包含 Redis HA failover、control-plane kill/restart、重复执行下的业务幂等验证和量化 SLO。 |

> 高吞吐采集不属于 G0/G1/G2 的放宽条件。D1 采用独立 ingress 或 SDK cluster transient 路径，审批工作流始终使用 durable/default mode。

## 2. 达成状态

> **2026-07-24 重签（以此为准，覆盖 2026-07-23 复审回退）**：已按修订版设计 §14.3 顺序完成重实现并验真——EventID 改 crypto/rand UUIDv4（跨进程唯一）；raw runtime event 以 `CollectedRuntimeEvidenceEvent`+`EvidenceRecordMeta` 包络（verifier 校验 `Meta.ExecutionID==Event.ExecutionID`）；`make test-g0-evidence-required` run_id 改随机 UUIDv4（非 commit SHA）；运行时查询 Redis `INFO server`/MySQL `SELECT VERSION()` 写入 typed `environment_observations`；verifier 强制 `relevant_tree_clean==true`（不再 mismatch-only）、environment 非空且来自 typed record 且一致、`required_rows==observed_rows>0`、`suite_records` 非空（由 test2json 流解析）、A0 每场景 `handler_invocations>0` 且引用 counter snapshot（OSKillSIGKILL phase-B 例外改 `system_task_delivery>=1`）、`run_identity` 一致性、`looksLikeCommitSHA` 拒绝 SHA 作 run_id；A0 commit-then-flush 改真 counting handler + 生产 commit 路径（非 direct `BuildTaskLease+CommitTaskResult`）、删固定 `QueueDeliveries:1`/`RecoveryTimeMS:0`/自报 `Pass:true`/synthetic os-kill artifact 行；A3 `db_deadlock`→`db_lock_wait`（MySQL 1205 lock-wait-timeout）命名修正（local-fake 真 1213 保留 `db_deadlock` 独立 contract）；新增 11 个 adversarial verifier 负例。在新 clean SHA `a75c483` 重跑 `make test-g0-evidence-required` verifier PASS：`environment{redis=8.6.2,mysql=9.7.1}`（运行时查询非空）、`suite{exit=0,skip=0,dropped=0,required_rows=20,observed_rows=20}`、`raw.suite_records` 非空、20 derived（5 A0 + 15 A3）、5 A0 `handler_invocations` 实测（commit-then-flush=1 / queue-handoff=2 / ack-loss=1 / request-loss=6 / os-kill-sigkill=0+system_task_delivery 协议观测 + accepted_commit/applied_advance receipt）、`source.commit_sha=a75c483`、`source.relevant_tree_clean=true`、`source_recomputed=true`、`suite_recomputed=true`、`verification.passed=true`、sidecar SHA-256 digest 匹配。§9.1 `go build ./...` + `go vet ./...` + engine/control/runner `-race` 全绿。G0 重新标为本地 PASS（evidence artifact 绑定 SHA `a75c483`，即 gate 运行时的 relevant-tree-clean 提交；后续 docs/cleanup commit 不在 `RelevantSourcePaths` 内，不改变 evidence 语义，故不以 HEAD SHA 为准）。**2026-07-24 Minor 清理后重跑**：解决最终审查 5 个非阻塞 Minor（checkA0 counter `HandlerName` introspection 关闭既有信任边界、删 `required_rows<=0` 死守卫、`checkEnvironmentIntegrity` 跨 run 过滤与 `recomputeEnvironment` 一致、`newRuntimeEventID` 简化为无参以符 spec §4.3.1、docs "current SHA" 措辞修正），在 SHA `5370565` 重跑 `make test-g0-evidence-required` verifier PASS，A0 counter `handler_name` 实测匹配 production delegate（commit-then-flush=`test.fault` / ack-loss=request-loss=`test.a0.start` / queue-handoff=`queue-handoff-consumer`，OSKillSIGKILL 仍走 `system_task_delivery`），artifact 绑定 `5370565`、`relevant_tree_clean=true`、20/20 rows、suite_records 非空、`verification.passed=true`。external CI/release signoff 仍 pending（§14.3 step 7）。下文 2026-07-23 复审回退与 `ca8ff0b` 表述均为历史记录，状态以此 2026-07-24 重签为准。

> **2026-07-23 独立复审强制回退（历史记录）**：机械读取绑定 `ca8ff0b` 的 final artifact 发现 `environment.redis_version==""`、`environment.mysql_version==""`、`suite.required_rows==0`、`suite.observed_rows==0`、`raw.suite_records==null`、五个 A0 derived `handler_invocations==0`、却 `verification.passed==true` —— sidecar digest 匹配只证文件完整、不证 gate 语义成立。复审结论：**REQUEST CHANGES / NO-GO；A0 PARTIAL；A3 PARTIAL；G0 FAIL**。`7599dc0`/`6a6407a` 依据该 artifact 的 G0 签出为 verifier 假阳性，已回退。根因（已逐条对照代码确认）：EventID 由进程内 atomic 序列生成（非 crypto/rand UUIDv4）；raw runtime event 裸存（无 `CollectedRuntimeEvidenceEvent`/`EvidenceRecordMeta` 包络）；`make test-g0-evidence-required` 以 commit SHA 作 run_id；verifier 仅比较 `RelevantTreeClean` 布尔、未强制重算为 true，且不检查 environment/rows/suite_records/A0 handler_invocations；A0 commit-then-flush 仍直接 `BuildTaskLease+CommitTaskResult`、`HandlerInvocations=appliedCommits` 逻辑估算、固定 `QueueDeliveries:1`/`RecoveryTimeMS:0`、自报 `Pass:true`。重实现须按修订版设计 `.claude/specs/2026-07-22-p0-g0-a0-a3-real-evidence-design.md` §14.3 顺序：先回退（本步）→ 实现 schema v3 typed raw records + run identity + 完整 verifier → 删除 A0/A3 direct/fixed/helper-derived 证据 → 单元/race/集成/adversarial 负例 → 在新 clean SHA 重跑 `make test-g0-evidence-required` → 人工核对 artifact 关键字段非空 → 方可重新标 G0 PASS。external CI/release signoff 仍 pending。下文带 `ca8ff0b` 的「已完成」表述均为历史记录，状态以上述为准。

### G0 — 调度可靠性候选 ✅ 已完成（本地真实运行证据，SHA a75c483；2026-07-24 重签）

> A0–A3 代码侧缺口均已闭合，本地 podman 证据（真实 Redis 6380 + MySQL 3306，含 SHA 绑定 + 结构化报告 JSON）满足退出条件。**2026-07-23 真实证据签出**：A0/A3 的 artifact 字段全部改为生产 runtime 边界实测（`RuntimeEvidenceBuffer` commit/advance/retry receipts + counting wrappers），不再有 fixture 名回填/常量/逻辑估算；独立 verifier（`test/integration/internal/evidence`）从 raw ledger 重算、atomic finalize versioned envelope；`make test-g0-evidence-required` 在 SHA `ca8ff0b` 上 verifier PASS（`verification.passed=true`、`source_recomputed=true`、`suite{exit=0,skip=0,dropped=0}`、20 derived observations = 5 A0 + 15 A3），final artifact `test/integration/testdata/evidence/evidence-ca8ff0b….json` + SHA-256 digest 绑定该 SHA、relevant tree clean。**2026-07-22 发布门重定义**：CI artifact 签出不再阻塞 G0 闭合，降为后续 TODO（见修复设计 §12）；external CI/release signoff 仍 pending。详见
> [sdk/server 生产就绪验收修复设计](../../.claude/specs/2026-07-18-sdk-server-production-readiness-remediation-design.md) §6 / §12 与 [P0-G0-A0-A3 real-evidence design](../../.claude/specs/2026-07-22-p0-g0-a0-a3-real-evidence-design.md)。

| 项 | 状态 | 缺口 | 既有 commit |
|---|---|---|---|
| A0 真实 Redis cyclic 崩溃恢复 | ✅ 已完成（本地真实运行证据，SHA a75c483；2026-07-24 重签） | A0 fault matrix（`test/integration/a0_fault_matrix_test.go`）覆盖 5 required scenario：commit-then-flush-before-delivery、**ReportAckLoss**（真实 runner report 链 + lossy `ProtocolClient`：转发→等 Accepted→丢响应→transport error，server 已 commit）、**ReportRequestLoss**（one-shot 丢首报→transport error→真实 `LeaseSweeper.SweepOnce` directory-cleanup-before-reclaim→新 runner 取新 authoritative lease 重执→旧 report 经 lease-token fence 零 mutation）、queue handoff（真实 asynq 跨进程消费）、**OSKillSIGKILL**（`TestA0OSKillSIGKILLRecovery`：独立 OS 进程经 `go test -c` 子进程、不可捕获 SIGKILL、A 经 IPC stdout 发 raw commit receipt→parent 核验 Accepted+Applied→SIGKILL→Redis outbox 与 IPC receipt 交叉核验→B 真实 dispatcher 恢复 advance intent；A 无 defer/stop/report）。**2026-07-23 real-evidence 改造闭合 2026-07-20 全部确定性缺口**：(1) OSKill 升级为真独立 OS 进程 SIGKILL（非优雅关闭）；(2) ReportAckLoss/ReportRequestLoss 用真实 runner + lossy proxy report 链（非直接调 BuildTaskLease/CommitTaskResult）；(3) `HandlerInvocations`/`DAGAdvances`/`QueueDeliveries`/`RecoveryTimeMS` 全部从 `RuntimeEvidenceBuffer` commit/advance receipts + counting wrappers + state/directory 实测（删常量/逻辑估算，纠正假值：CommitThenFlush DAGAdvances 2→1、QueueHandoff DAGAdvances 1→0）；(4) cyclic commit 路径（`commitLegacyNode`）补发 commit/advance receipts（Option B：持久化 CyclicOutbox 即 cyclic advance），使 SIGKILL execution 产 `checkA0` 可验证的 RuntimeEvents。独立 verifier（`test/integration/internal/evidence`）从 raw ledger 重算 derived observations、atomic finalize versioned envelope；`make test-g0-evidence-required` 在 SHA PASS，final artifact `evidence-ca8ff0b….json` + SHA-256 digest 绑定该 SHA、`verification.passed=true`。结构化报告仍写 `test/integration/testdata/a0_fault_matrix_report.json`。**TODO（不阻塞）**：真实 CI run 的 artifact 证据（external CI/release signoff pending；2026-07-22 发布门重定义降为后续 TODO） | `4a04057` + T6（旧 4-scenario 基础）+ `6e67674`..`ca8ff0b`（runtime evidence + 5-scenario real-evidence 改造 + 独立 verifier + make target）；2026-07-20 重新验收 + 2026-07-23 real-evidence 闭合 |
| A1 control-plane binder fail-closed | ✅ 已完成（本地证据） | control-plane 已只走 `TaskHandlerBinder`，`BindTaskHandler` 传播 consumer start error；T2 修复 SDK `newFromConfig` 类型断言优先 `StartBinder`（`StartBinding(eng)(func(),error)`），legacy `Bind` 降级为 deprecated 兼容 adapter（once-only 警告）；bindHandler rollback 补 `tm.Stop()`+`<-tmDone`+`b.rdb.Close()`（消除 monitor goroutine 与 Redis client 泄漏），正常 stop 改为 stopConsumer 先 → cancelOutbox/<-outboxDone → tm.Stop/<-tmDone → transport/rdb/pool close。**2026-07-20 重新验收发现 ResourcePool 泄漏，已在 `0d6440b` 代码侧闭合**：新增统一 `closeOwnedResources(startupErr)` helper（transport→rdb→pool，有界 10s context，`errors.Join` 不吞 startup error），`bindHandler` 三失败分支（consumer start / afterConsumerStart / afterOutboxStart）均调用；`New` `:289` AsAsynqConnOpt 失败前补 `rdb.Close()`（修 rdb 泄漏）；5 新测试覆盖三分支 Close calls==1 + Close 自身 error 不掩盖 startup + nil-pool noop。**2026-07-21 最终全分支复核补充（R2.1）**：正常 shutdown 错误现已可观测：`nonConsumerStop` 与 consumer `bindHandler` stop 路径均通过 `sync.Once` 精确一次采集 `transport.Close()` / `rdb.Close()` / `resourcePool.Close(ctx)` 错误，经 `ShutdownReport` 投递给 `ShutdownObserver`；未配置 observer 时回退到 `engine.Logger` → stdlib `log`。内置日志路径对 URL/DSN 凭证（`scheme://user:pass@host`）及常见查询参数（`password`/`pwd`/`secret`/`ak`/`sk`）做保守脱敏，observer 仍接收原始错误；同时 `reportShutdown` 增加 recover，防止 panicking observer 拖垮 shutdown。**保留**：CI artifact 签出降为后续 TODO（2026-07-22 发布门重定义，不阻塞 G0）；pool 所有权属 SDK/caller，`New` 失败时注入的 pool 关闭超本任务范围 | `5ee3414` + `162ae3d` + `6a8b537` + `818379b` + `0d6440b` + `1377b26` + `40191f1`；2026-07-20 重新验收 + R2/R2.1 修复 |
| A2 dead-letter 原子处置契约 | ✅ 已完成（本地证据） | 结构化请求、成功 receipt、并发幂等、execution/node/activation guard 和 manager 基础已实现；T3 fail-closed missing meta——移除 `if nodeName~=''` 绕过，缺 node/activation → outcome 7 `rejected_metadata_missing`；拒绝 outcome 写不可变 receipt（readReceipt 返回 outcome，Lua 首段返回 stored outcome）；opaque 签名 cursor `v1.<b64 payload>.<b64 sig>`（32B random HMAC key，verify-before-trust + hmac.Equal 常量时间），(score,member) 稳定续页（ZRangeArgsWithScores 原子），非法/篡改/跨 exec/过期→ErrCursorExpired。T4 management 注入共享 DeadLetterManager（sync.Once 守护 lazy init，metrics/audit 非 nil）；API+CLI 统一 outcome counter `xflow_outbox_dead_letters_replayed_total{tenant,outcome}`；CLI 默认走 HTTP management API（token 仅 Authorization header，redactURL 脱敏），break-glass Redis-direct 显式 + 独立身份；Engine.ReplayDeadLetter doc 标注生产 manager-only；SQL schema 加 node_id/activation_id/entry_id/receipt_audit_id + idx（INFORMATION_SCHEMA-guarded ALTER 幂等回填），ReceiptProjector 幂等 AppendAuditIfAbsent，bounded retry 3 指数 + OnOutboxError alarm，dead_letter_reconcile 差集扫描幂等补投影不重执行。**2026-07-20 重新验收发现 fail-open（nstatus nil 跳过 guard / currentActivation=='' 跳过），已在 `5a44fc9` 代码侧 fail-closed**：nstatus nil/未知值 + currentActivation=='' → outcome 7 `rejected_metadata_missing`（复用 outcome 7，不新增 code）；terminal 值（success/failed/skipped/canceled/continued）仍走 outcome 4 保留区分；合法 NodeStatus allowlist 调研 10 值全覆盖（terminal 5→4 / eligible non-terminal 3 running/committing/waiting→activation guard / excluded non-terminal 2 pending/suspended→7）。fail-closed 不执行 dead→ready move，写不可变 receipt（不记 token/payload），同 RequestID 恢复同 audit_id+outcome。真实 Redis 5 场景（缺 execution status/node:status/node:meta/activation_id field/bogus status）+ 8 goroutine 并发无穿透（leaked==0/rejected==8/dead==1/ready==0）。**2026-07-21 重新验收发现 advance dead-letter 不可恢复**：真实 advance entry 的 source 必然 terminal（commitNodeLua 原子写 source terminal + advance outbox），旧 guard 无条件 terminal→outcome 4 使后继节点永远无法推进；已在 `f4f962a` 代码侧按 intent 分支：advance 要求 source terminal（fall-through 到 activation guard，幂等由 advance marker 保证），非 terminal/absent source for advance→outcome 7；非 advance intent 仍 terminal→4。新增 `TestReplayDeadLetterAdvanceRealLifecycle`（真实 `Store.CommitNode` 产生 advance dead-letter→replayed），`advance_no_node_status`（不可能状态）改为 `advance_source_terminal`。**2026-07-21 最终全分支复核补充（R1.1）**：`advanceNodeLua` 在 terminal 检查之后新增 activation fence，读取 source node `activation_id` 并与任务携带的 activation 比对，不匹配时直接在设置 advance marker 之前返回 0，避免错误推进下游计数器；`engine.handleSystemTask` 增加 defense-in-depth 判断 `node.ActivationID != task.ActivationID` → 静默丢弃，确保即使 outbox redelivery 越过 Lua fence 也不会重复推进 DAG。新增 `TestEngineHandleSystemTaskDropsStaleActivationAdvance` 在 miniredis 真实执行上复现 source activation 从 0 被刷到 1 后旧 activation 0 的 advance 被丢弃且下游计数器/队列无变化。**保留**：CI artifact 签出降为后续 TODO（2026-07-22 发布门重定义，不阻塞 G0） | `fe15676` + `e2a9def` + `9cee3f1` + `c9a59ae` + `416dc2d` + `94d801d` + `5a44fc9` + `f4f962a` + `0689d52` + `fc74ebb`；2026-07-20 + 2026-07-21 重新验收 + R1/R1.1 修复 |
| A3 跨进程错误分类与兼容语义 | ✅ 已完成（本地证据，SHA a75c483；2026-07-24 重签；1205 命名修正为 db_lock_wait） | wire DTO、HTTP/gRPC/MySQL/script/function classifier、生产 runner ResourcePool/credential wiring 已实现；**三拓扑 parity 矩阵**（local embedded + server-runner + **durable SDK cluster**）已落地于 `test/integration/action_parity_*.go`，同 fixture 的 attempt/status/port/`error_code`/`error_kind`/`error_retryable`/`handler_invocations`/DAG 推进断言：**HTTP/gRPC/script 跨三拓扑统一；Database 跨 server-runner↔cluster 统一，local-fake 单独 contract**（`assertParityAll`/`assertParityThreeWay`），机器可读矩阵 artifact 经 `PARITY_MATRIX` 日志行输出（每 fixture × 三拓扑行，含非空结构化字段）。**2026-07-22 R6 闭合**：(1) gRPC 从两拓扑 `assertParity` 升级为三拓扑 `assertParityThreeWay`（`grpcPoolWrapper` 自注入 pool，cluster 路径零生产改动）；Database 补 cluster-durable 拓扑，server-runner↔cluster real-MySQL 精确 parity（`databaseParityHandler` 自注入 cred+pool，无 `WithCredentialResolver` 生产改动），local-fake 与 real MySQL 的已知 code 分叉（bad-conn `database.connection_lost` vs `database.unknown`、deadlock 1213 vs 1205）保留在 `TestDatabaseActionErrorParity` 单独 contract 断言并文档化，不宣称三拓扑同构。(2) `ParityOutcome` 补 `HandlerInvocations`（实测，fixture 计数器经 `parityFixtureBuild` per-topology 隔离导出）；`ErrKind`/`ErrRetryable` 从 runtime commit receipt 实测（`applyActualFromClassification` drain 该拓扑 `RuntimeEvidenceBuffer` 选唯一 `Applied==true` commit event 派生，**production-derived 非 fixture 回填**）；`stampExpectedKind`/`parityKindFromName` 已删除，`WantKind`/`WantRetryable` 仅作 contract oracle（比较方向 `runtime receipt actual vs manifest expected`）；`assertParityAll` 增 kind/retryable/invocations 跨拓扑 parity + 契约断言。(3) artifact `error_kind`/`error_retryable`/`handler_invocations` 列现带真值。gRPC `localhost:1` 改 `127.0.0.1:1` 消除 IPv4/IPv6 DNS 分叉导致的 ErrStr 误判。**保留**：`error_kind`/`error_retryable` 来自 runtime receipt 实测（production-derived）；CI artifact 签出降为后续 TODO（2026-07-22 发布门重定义，不阻塞 G0）。**PostgreSQL 收窄**：当前无 typed error classifier、无运行集成证据，G0/G1 不在支持范围（见 §A3 注）；不靠 MySQL 结论外推。MySQL classifier 的真实集成证据见各 classifier 测试 | `1995148`..`69fdcde` + parity 三拓扑提交；2026-07-20 重新验收 + 2026-07-22 R6 闭合 |

> G0 退出条件见修复设计 §12。**2026-07-24 重签**：已按修订版设计 §14.3 顺序重做，在新 clean SHA `a75c483` 重跑 `make test-g0-evidence-required` verifier PASS，artifact 关键字段（environment/rows/suite_records/A0 handler_invocations）全实测非空、sidecar digest 匹配，§9.1 build/vet/race 全绿；G0 重新闭合（本地真实运行证据，SHA `a75c483`）。external CI/release signoff 仍 pending（§14.3 step 7）。

### G1 — 受限单租户生产 ⛔ 未满足

> G1 依赖 G0。G0 已重新闭合（本地真实运行证据，2026-07-24 重签，SHA `a75c483`）；G1 阻断项转为 B1/B3 自身缺口（B3 的 R3 reconcile 收敛为确定性代码缺口，非 CI 问题）。

| 项 | 状态 | 缺口 | 既有 commit |
|---|---|---|---|
| G0 基线 | ✅ | G0 已完成（本地真实运行证据，SHA `a75c483` 2026-07-24 重签；独立 verifier PASS，environment/rows/suite_records/A0 handler_invocations 全实测非空，sidecar digest 匹配，§9.1 build/vet/race 全绿；external CI/release signoff pending） | 见上 |
| B1 OTel 端到端接线 | 🟡 代码侧已修复，G1 整体未签出 | T7 修复：gRPC `ReportResultRequest` proto 加 additive `map<string,string> trace_carrier=5`，`make proto` 重生成，ToProto/FromProto cloneLabels round-trip；补 `xflow.workflow.submit/invoke`、`task.report`、`outbox.flush`、`outbox.replay` spans；dispatch span 在 BuildTaskLease 之前启动、覆盖 FinalizeClaim，parent 从 Redis 持久化 W3C carrier 经 ExtractCarrier 取（真实 remote parent）；`GRPCStreamServerInterceptor` + tracedServerStream override Context()；baggage 默认关，启用装 FilteredBaggagePropagator（denylist:tenant/token/payload/secret/ak/sk + MaxValueLen 1024 + MaxEntries 16 + MaxTotalBytes 8KiB）；DumpSpans + in-memory exporter 断言 HTTP/gRPC unary/gRPC stream 完整 parent/link graph。engine 加 lazy outboxTracer()。**2026-07-20 重新验收发现 submit→dispatch parent graph 断链（pollTask 未注入 tenant 到 ExecutionTraceCarrier），已在 `2ed24a1`+`042b28f` 代码侧闭环**：pollTask 用 `claim.Assignment.TenantID`（权威值，非 client）注入 ctx，ExecutionTraceCarrier 从正确 namespace 取 carrier；修复中发现更深根因——`reportResult` 同缺口（gRPC/HTTP report 路径无 principal resolver，`CommitTaskResultWithOutcome` 读 default namespace，tenantA commit 静默 `CommitOutcomeExecutionInactive`，workflow 卡死 running），通过加 `TaskLease.TenantID`（lease JSON payload `tenant_id,omitempty`，**非 W3C baggage**，纯 additive 向后兼容——旧 runner 忽略未知字段、旧 lease 空值回退 default）解决：pollTask 三路径（durable replay/normal BuildTaskLease/lease-already-active recovery）设 `lease.TenantID`，`reportResult` 顶部 `tenant.WithTenant(ctx, req.Lease.TenantID)` 注入。WARN 降级**全部移除**改 `t.Fatalf` 强断言；tenantA trace graph（5 spans 同 TraceID + 4 parent 边）+ 并发 tenantA/B carrier 不串测试绿；artifact `one_trace_id=true`/`dispatch_parented_to_submit=true`/`tenant_a_one_trace_id=true`/`cross_tenant_carrier_isolated=true`（t.Fatalf 守卫后置 true）。tracing shutdown 5s 有界 context + flush error log。span 命名审查无冲突（`xflow.task.report` 仅 server 端，runner 端 report 是 gRPC client call 无命名 span）。**2026-07-21 重新验收发现 report authority 仍 fail-open**：`c725cde` 加了 `LookupLease` 但 `ok=false`/目录未实现 `LeaseLookup` 时回退 client 可变 `req.Lease`，runner-a 可报告 runner-b 的有效 lease 并跨 tenant commit（确定性 HTTP 探针复现）；已在 `b2ff782`+`76c2f1c` 代码侧 fail-closed：`LeaseLookup` 成为 report 路径强制能力，鉴权/session validation 前置于目录查询，`ok=false` 或无 `LeaseLookup`→`ErrInvalidLeaseToken`（HTTP 409，不 commit、不选 tenant namespace），移除回退；`noLookupDirectory`（嵌入因而隐式实现 `LeaseLookup` 的假覆盖）替换为 `lookuplessDirectory` 命名字段 wrapper + cross-runner lease-swap 探针。**保留（诚实）**：(1) outbox dispatcher 背景 drain path（`engine/atomic.go:449` 用无 tenant 背景 ctx 调 FlushOutbox）pre-existing tenant gap，仅影响崩溃后 tenantA outbox 后台恢复（正常同步提交路径 `flushInitialOutbox` 带 tenant 不受影响），超 R4 范围；(2) CI collector graph artifact 签出降为后续 TODO（2026-07-22 发布门重定义；本地 in-memory exporter 断言已覆盖 trace graph） | `0b21125` + `1815635` + `3ded452` + `2ed24a1` + `042b28f` + `c725cde` + `b2ff782` + `76c2f1c`；2026-07-20 + 2026-07-21 重新验收 + R4 修复 |
| B3 API authz + fail-closed | 🟡 部分完成 | Principal、operation/resource Authorizer、默认 deny、tenant IDOR、mutation admission audit 和 SQL append-only sink 已实现；T8 修复：execution 子路由 `resolveExecutionRoute` 解析提前到 authz 之前，inspect/wait→execution.read（非 mutation），signal/revoke-signal/cancel→各自 operation + mutation admission audit（fail-closed，handler 前）+ outcome audit（handler 后，sync 非 defer）；未知 verb→deny 404；management leader/runner/executions 统一 authzWrap + 独立 scope；healthz/readyz 最小公开信息；`validateProduction` 默认 `--mode production` fail-closed（缺 PrincipalAuth/单 token/nil Authorizer/非 durable AuditSink/nil Reconciler 之一即启动失败）；生产拒绝单 token 全 scope，dev 单 token 给固定 operator scope 集；token→principal/scopes registry。T9 修复：audit event 区分 immutable admission/outcome/reconciled phases（显式 phase 列 + outcome-scoped phase_key UNIQUE 索引，INFORMATION_SCHEMA-guarded ALTER）；worker `ListUnreconciledAdmissions` 扫超时 admitted-no-outcome，`ExecutionAuthority.Probe` 只读 GetExecution（不重执行 mutation）；leader gating 复用 RedisLeaderElector + per-sweep 退避 + ReconcileMetrics；幂等 defense-in-depth（NOT EXISTS 扫描 + check-then-append + uk_phase_key）。T4 ReceiptProjector 幂等 AppendAuditIfAbsent + dead_letter_reconcile 差集扫描。**2026-07-20 重新验收发现的决定性缺口（crash-safe reconciliation 未闭环）**：(1) workflow create/invoke 的 authz resolver 为 nil，admission 写入时 `ExecutionID` 为空，执行完成后 wrapper 也未从 response 回填 correlation；(2) `ExecutionAuthority.Probe` 对空 ExecutionID 直接返回 `Indeterminate`；signal/revoke/cancel 即使 execution 存在也固定 `Indeterminate`，无 operation receipt 或可判定权威状态；(3) 进程在 mutation 成功后、outcome append 前崩溃时 worker 无法可靠收敛；(4) G1 artifact `reconciled_by_worker: 0` 但测试未要求 >0 仍通过；(5) 真实 MySQL reconcile 专项失败：库已有 10080 条 pending admission，oldest-first `LIMIT 64` 未返回本轮记录（暴露测试隔离 + backlog 可运维性）；(6) G1"宿主幂等"artifact 此前只是 `>=1 (at-least-once, host-side dedup)` 字符串，制造的是重复 commit 而非 `handler invocations >=2` 下真实外部副作用唯一 — **2026-07-22 已升级为实测强证据**：`g1RunIdempotencyReport` 以带 `UNIQUE(execution_id,node_name)` 约束的 MySQL 业务表为真实外部副作用，handler 用宿主暴露的稳定身份（`types.Input.ExecutionID`+`NodeName`）作幂等键，对同一 task 二次调用 handler（at-least-once 重投），实测 `handler_invocations=2`、`business_rows=1`、`host_fence=duplicate_terminal`（`INSERT IGNORE` + 重复 commit 被 host fence）。即"worker 框架/SQL phase 已实现，但多数 mutation 权威 correlation/receipt 未完成"（R3 修复，本轮范围外）。**保留**：G1 整体未签出 | `d3bf6ba` + tenant boundary commits + `135c8c2` + `b884e90` + `3502038` + `a83ac14`；2026-07-20 重新验收 |

> G1 退出条件见修复设计 §12。G0 已重新闭合（本地真实运行证据，2026-07-24 重签，SHA `a75c483`）；G1 阻断转为 B1/B3 自身缺口，B3 的 R3 reconcile 收敛（mutation authority correlation/receipt）为确定性代码缺口，未完成前不得宣称 G1。

> **G1 生产 e2e 覆盖（T10 + R4）**：`test/integration/g1_production_e2e_test.go` 在真实 Redis + MySQL 下覆盖 production-auth 全链路：PrincipalAuth + TenantAwareAuthorizer + SQLAuditSink + AuditReconcileWorker + Metrics + Tracer + RequireWorkflowAuth + WithManagement。HTTP 入口 submit/invoke/signal/revoke-signal/cancel 的 allow/deny 矩阵 + 跨租户 IDOR → 404（不泄漏存在性）；gRPC runner Connect + ReportResult 端到端；B1 trace graph **强断言已落地（R4）**：5 span 存在且同 TraceID + 4 parent 边 `t.Fatalf`（WARN 降级已移除）；default + tenantA 双链均覆盖（`g1RunGRPCRunnerConnectReportForTenant`），并发 tenantA/B carrier 不串（`g1RunCrossTenantCarrierIsolation`，三重防串断言）；artifact `one_trace_id=true`/`dispatch_parented_to_submit=true`/`tenant_a_one_trace_id=true`/`cross_tenant_carrier_isolated=true`（t.Fatalf 守卫后置 true）。复杂审批 DAG（会签 quorum、timer、cancel、cyclic reset、重复 signal → 409）；dead-letter replay HTTP 合约；T9 reconcile 幂等；metrics scrape。**幂等强证据已落地（2026-07-22）**：`g1RunIdempotencyReport` 用带 `UNIQUE(execution_id,node_name)` 约束的 MySQL 业务表作真实外部副作用，handler 以宿主稳定身份（`types.Input.ExecutionID`+`NodeName`）为幂等键，对同一 task 二次调用 handler，实测 `handler_invocations=2` + `business_rows=1` + `host_fence=duplicate_terminal`（`INSERT IGNORE` + 重复 commit 被 fence），artifact 记录实测值而非 `>=1` 字符串。**仍为弱证据（R3 未做）**：reconcile（`reconciled_by_worker: 0` 但测试未要求 >0 仍通过）。artifact JSON `test/integration/testdata/g1_e2e_report.json` 不入库（.gitignore + os.Remove）。测试覆盖既有行为 + R4 的 pollTask/reportResult tenant 注入；G1 整体状态仍为 ⛔（G0 已闭合 + R8 已闭合；阻断项为 B3 R3 reconcile 收敛未做，且 reconcile 弱证据需 R3 升级为强断言）。

> **R8 真实 binary e2e（2026-07-22 闭合）**：`test/integration/r8_binary_e2e_test.go` 是唯一启动生产 `bin/server`+`bin/runner` 真实 OS 进程的 e2e（其余 e2e 皆为进程内 `apiserver.New`+`httptest` 或 `go test -c` 测试二进制）。`go build ./cmd/server`/`./cmd/runner` 构建到 `t.TempDir()`，生产模式（`-mode production` + 真实 Redis + 真实 MySQL + `-auth-tokens-file` 0600 + `-require-api-auth` + `-management` fail-closed）启动 server，`/readyz` 探测就绪后启动 runner（HTTP transport，`--transport http`，cobra/pflag 双横线长 flag）。三子测试：①`HappyPath` 提交 `xflow.function`（inline Expr code）→ terminal Success（真实 binary 全链路）；②`KillRunnerWhilePendingThenRestart` SIGKILL runner、无 runner 期间提交（短窗口断言非 terminal 证明确无处理）、重启 runner → pending execution → Success（durable Redis 状态存活 + 新 runner 进程从 asynq 取回排队任务，确定性核心证据）；③`KillRunnerMidFlightThenRestart` 节点 Running 时 SIGKILL runner、重启、轮询收敛——asynq active-task 恢复延迟不可控，30s 窗口未收敛则 `t.Skip` 降级（不阻断；确定性证据在 ①②）。R8 期间发现并修复真实生产缺陷：`handleSubmitWorkflow`/`handleInvoke` 未调 `engine.WithWorkflowDef`，生产模式（distributed backend 接 MySQL store 时）`buildExecutionRecord` 写 `workflow_def`（NOT NULL）为 NULL → submit 500；G1 in-process harness 因 `distributed.New(addr, nil, ...)`（store=nil）跳过 SQL execution 投影而掩盖此 bug。修复：两 handler 提交前 `ctx = engine.WithWorkflowDef(ctx, req.Workflow)`。R8 测试即该缺陷的回归守卫。

### G2 — 多副本 HA / 多租户 ⏳ 未完成

| 项 | 状态 | 说明 |
|---|---|---|
| B2 control-plane HA + Redis HA soak | ⏳ | 代码侧：leader election/graceful shutdown/management 端点已具备；**Redis HA 客户端代码已完成**（Task 1.1–4.1：`redis.UniversalClient` 宽化 + `RedisConfig` single/sentinel/cluster + `WithRedisConfig` Option + asynq `AsAsynqConnOpt` 三模式映射 + `cmd/server --redis-mode` flag + `apiserver.Config.RedisConfig` 透传 + sentinel 认证字段，commits `9a39996`..`c292d7d`）；**workflowreg/trigger cluster-safety 已修**（Task 2.1 hash tag + Task 2.2 单 key 核查，commits `aed4db3`/`1c54be0`）；**soak 脚手架与报告类型已完成**（Task 5.1 harness + 5.2 in-process graceful 场景/ENV-GATED stub + 5.3 SLO 报告类型与模板，commits `dc13301`..`065974a`）；Redis failover、network partition、runner kill、report response loss、outbox flush fault 仍未在真实环境执行。**仍缺（ENV-GATED）**：真实 sentinel/cluster Redis 环境连通性/failover 验收、多副本 soak 报告填实、SLO 量化达标判定、真实 cluster 下 CROSSSLOT 回归。详见 [ha-soak-plan](../references/ha-soak-plan.md) 与 [ha-soak-report-template](../references/ha-soak-report-template.md) |
| 多租户 tenant boundary | ⏳ 代码与测试完成 | tenant boundary 全链路代码已完成（Phase 6-8）：`backend/tenant` context 原语 + `WorkflowDef.TenantID`（`json:"-"` 编译期禁止不可信客户端，commit `726e6df`）；rstate/workflowreg/trigger key 全部 tenant 前缀（`xflow:t<tenant>:...`，无花括号保 hash tag，Task 7.1/7.2）；API 层 principal 签发 + `TenantAwareAuthorizer` + IDOR（请求体 tenant 忽略，跨 tenant → 404 不泄漏存在性，Task 7.3）；audit/metrics/trace tenant 标签 + 高基数防护（Task 7.4）；runner placement + credential tenant scope + asynq payload tenant（Task 7.5）；dead-letter/outbox manager 双保险 + CLI `--tenant`（Task 7.6）；越权测试矩阵 `test/security/tenant_isolation_test.go` 10 场景全绿（Task 8.1，miniredis，非 ENV-GATED）。**仍缺（ENV-GATED）**：真实多租户部署的端到端验收（多 principal 并发、跨 tenant 性能隔离基线、真实 Redis Cluster 下 tenant key 分布）。详见 [tenant-boundary-design](tenant-boundary-design.md) |

> leader election、hash tag 或 namespace 单独存在都不等同于 control-plane HA 或多租户隔离。本轮 G0/G1 修复不得顺带宣称 G2。

#### G2 control-plane HA 承诺与边界声明

G2 control-plane HA 的承诺范围与限制（映射 §4 反声明，在 G2 整体达成前不得对外宣称）：

- **承诺 at-least-once，不承诺 exactly-once**：handler 与 Runner Protocol 保持 at-least-once；failover 或 lease replay 可能重复 invocation，业务副作用只产生一次依赖宿主幂等键兜底（映射 §4「failover 不重执行」反声明）。
- **leader election 仅协调 leader-only maintenance**：`RedisLeaderElector` SETNX + Lua 续约/释放只用于 gate `lease_sweeper` 等维护任务，不提供完整 HA SLO；「leader election 等于 control-plane HA」是反声明（§4）。
- **Redis HA 客户端代码就绪 ≠ control-plane HA 已验收**：`UniversalClient` 宽化与 sentinel/cluster 构造代码落地（Task 1.1–4.1）只保证"可配置 sentinel/cluster 模式"，真实多副本 soak + SLO 量化是 ENV-GATED；未填实 [ha-soak-report-template](../references/ha-soak-report-template.md) 前 G2 不得标完成。
- **hash tag / namespace / leader election 单独存在 ≠ HA 或多租户隔离**：hash tag 只保证 Redis Cluster 下 key 共置同 slot（不触发 CROSSSLOT），不是 HA 承诺；namespace 是命名空间隔离，不是安全边界；leader election 不等于 control-plane HA。三者均映射 §4 反声明。

### C — 内部抽象加固（非门槛）

| 项 | 状态 | 缺口 | Commit |
|---|---|---|---|
| C1 store GORM tags 下沉 | ✅ 完成 | `store/models.go` 已无 `gorm:` tag 与 `TableName()`；ORM schema 完全下沉至 `store/sqlstore` 的 `dbExecution`/`dbNode`/`dbSignal` | `a36f19b` |
| C2 Graph 深层不可变性 | ✅ 完成 | T1 修复 `validateReflectDomain` fail-closed 对任意含未导出字段 struct 报错（含字段路径）；`cloneReflectValue` 改为按字段逐个递归复制（不再浅拷贝 struct 后选择性深拷贝，消除未导出 alias）；pointer/func/chan/非字符串键 map 拒绝不变。外部包 `immutable_unexported_test.go` 复现 Compile-input 与 NodeAt-accessor 双 alias 探针并断言 Compile 源头拒绝。caller 审计：`types.Error.Timestamp`/`types.Trigger.Time` 为运行时元数据 struct，不进 Parameters/Vars/Config 值域 | `7355570` + `a6ead44`；2026-07-20 更新 |

### D — 高吞吐路径（独立架构，不阻塞审批 release gate）

| 项 | 状态 | 说明 |
|---|---|---|
| D1 采集与审批工作流分离 | 🟡 架构/采样就绪，容量基线未完成 | 两条独立参考架构、`make perf-sample`、p50/p95/p99 和结构化 `perf.metric` 已实现；`.github/workflows/perf-sample.yml` 已配置每日/手动非门槛采样与 artifact 上传，但当前 release evidence 未附可核验成功 run/artifact。受控 host 报告仍是空模板；**两拓扑多样本报告填实前只能称“架构与采样准备完成”**。审批始终使用 durable mode，不得用 transient 数据承诺审批产能 |

## 3. G1 部署承诺与限制

> 以下为 G1 达成后的目标承诺。**当前 G1 未满足**（见 §2），以下承诺在 G0 + B1 + B3 全部满足三层证据前不得对外宣称。

**达成后可宣称**：
- 在可信网络、单租户、允许控制面维护窗口的部署下，复杂审批工作流（会签、超时、取消、循环退回、重复 signal）生产可用。
- 调度意图不丢失：durable assignment/lease/outbox 在 Redis 中持久化，控制面重启后恢复；重复提交不重复推进 DAG（fencing）。
- handler 与协议 at-least-once；业务副作用由宿主幂等键保证。

**不可宣称**（见 §4 反声明）：
- 无中断 HA SLO（单副本 G1 部署需维护窗口）。
- handler exactly-once。
- 多租户隔离（G1 是单租户边界）。

## 4. 文档反声明（Release Check）

以下声明**禁止**出现在任何 release 文档中，均已证伪：

- ❌「P1-0 即生产可用」—— G0 只是调度可靠性候选，必须通过 G1 或 G2 才能宣称生产可用。
- ❌「failover 不重执行」—— 系统是 at-least-once，failover 可能重复 invocation，由业务幂等键兜底；不承诺不重执行。
- ❌「trace_id/span_id 可直接充当 OTel parent」—— `ExecutionSnapshot.TraceID/SpanID` 只是审计/检索字段，不是可跨进程恢复的 OTel `SpanContext`；远端 parent 必须用 W3C `traceparent` carrier。
- ❌「只读 replay」—— dead-letter replay 是特权写操作（dead→ready 原子转移），不是只读；必须有权限控制和审计。
- ❌「leader election 等于 control-plane HA」—— leader election 只协调 leader-only maintenance，不提供完整 HA SLO。
- ❌「namespace 是安全边界」—— namespace 和 runner labels 不是安全边界；多租户需独立 tenant boundary 实现。
- ❌「测试被 skip 即 integration 已通过」—— integration test 因依赖不可用 Skip 只能记为"未执行"，不能记为通过；CI 缺依赖时必须 fail-fast。
- ❌「字段 private 即 Graph 深层不可变」—— 字段私有化只是第一步；accessor 浅拷贝仍会泄漏可变引用，必须深层 defensive copy + Compile 校验才可称 deep immutable。
- ❌「单一共享 token 即 API authz 完成」—— 静态 bearer 只是单租户参考实现；必须有 principal + 资源/操作级 authz + 不可变审计，不得 allow-all。

### 4.1 tenant boundary 诚实性声明

tenant boundary 全链路代码与越权测试已完成（Phase 6-8，详见 §2 G2 行），但在 G2 整体达成前不得对外宣称"多租户隔离已完成"。以下边界声明在真实多租户部署验收（ENV-GATED）前不得放宽：

- **tenant 前缀 ≠ 加密隔离**：Redis key 前缀 `xflow:t<tenant>:...` 只是命名空间隔离，**不是密码学隔离**。跨 tenant 隔离依赖**服务端签发 TenantID（principal，忽略请求体 tenant）+ 全链路 context 校验 + 越权测试**，Redis 层是命名空间隔离不是密码学边界。任何能直接访问 Redis 的组件（如运维 CLI 直连）不受 tenant boundary 保护。
- **runner labels 不是安全边界**：runner placement 用**显式 tenant 归属**（`Assignment.TenantID` + runner 注册的 tenant 列表 + `ClaimForRunner` 过滤），**不得用 `RunnerSelector.MatchLabels` 兜底承载 tenant**。runner label 是调度提示，非隔离机制。
- **store key 前缀是主防线，manager/API 校验是双保险**：跨 tenant execID 在 rstate key 层因前缀不同直接 NotFound；API 层 `Inspect` 兜底 + deadletter manager 显式 `principal.TenantID == tenant.FromContext(ctx)` 校验是 fail-closed 第二防线。任一防线被绕过不构成隔离失效（纵深防御）。
- **跨 tenant → 404，不返回 403**：跨 tenant 资源访问一律 NotFound，不泄漏资源存在性（映射组织安全策略 §1「不泄漏 user 是否存在」）。
- **tenant 是低基数维度**：tenant 作为 metric label / span 属性，基数 = tenant 数量（G2 规模数十），可接受；execution ID / node name 组合 / lease token / runner ID **禁止作为 metric label**（高基数）。tenant **不得放入 W3C baggage** 跨进程传播（跨 tenant 泄漏风险），只作 span 属性。
- **default tenant 向后兼容**：未配置 tenant 时所有请求归 `default`，行为等价 G1 单租户；`default` 始终在 `xflow:tenants` SET 中（append-only，不回收，防 sweeper 漏扫孤儿 key）。
- **leader 全局不 per-tenant**：control-plane leader 保持全局单 key `xflow:leader:control-plane`，避免 N 倍 election 开销；leader-only maintenance 按 tenant 迭代（`xflow:tenants` SET）。
- **代码完成 ≠ G2 验收完成**：tenant boundary 代码 + 越权测试完成只解除 G2 退出清单中"tenant boundary 全链路 + 越权测试"一项；G2 整体达成仍依赖 G1 全满足（当前 ⛔）+ Redis HA + 多副本 SLO（ENV-GATED）。

## 5. 配置要求清单

G1 生产部署必须配置以下能力，详细示例见 [deployment-examples.md](../references/deployment-examples.md)：

| 能力 | 配置 | runbook |
|---|---|---|
| workflow API 鉴权 | token→principal/scopes 映射 + `--require-api-auth`；单一全 scope token 不满足最终 G1 | — |
| durable audit | `--mysql-dsn ...` 或等价持久 sink；内存 audit 仅开发使用 | — |
| audit reconciliation | crash-safe admission/outcome phase 区分 + leader-gated reconcile worker 框架 + dead-letter receipt projector 已实现；**但 workflow create/invoke/signal/revoke/cancel 的权威 correlation/receipt 未完成**（admission 写空 ExecutionID、Probe 对多数 mutation 固定 Indeterminate、reconciled_by_worker=0），阻断 G1 | — |
| runner 协议鉴权 | `--auth-policy runners.yaml` | — |
| 传输安全 | `--tls-cert`/`--tls-key`/`--tls-client-ca`（mTLS） | — |
| 分布式 tracing | `--trace otlp --trace-endpoint ...` | — |
| metrics | `--metrics-addr :9090 --metrics-path /metrics` | [dead-letter-runbook](../references/dead-letter-runbook.md) |
| structured logging | `--log-format json` | — |
| 维护窗口 | SIGTERM graceful shutdown | [maintenance-window-runbook](../references/maintenance-window-runbook.md) |
| Redis 备份/恢复演练 | 部署侧 | [maintenance-window-runbook](../references/maintenance-window-runbook.md) |
