# @xflow/api-client

Replaceable HTTP transport, AbortSignal support, unified error taxonomy, ETag/409 mapping.

## Dependency constraints

- Depends on `@xflow/workflow-core`.
- Does not read environment variables.
- Does not hold Umi runtime state.
- Does not create global singleton instances.
- No Umi / Ant Design / ProLayout dependencies.
