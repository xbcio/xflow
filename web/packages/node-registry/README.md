# @xflow/node-registry

Frontend `WorkflowNodePlugin` definition, descriptor, renderer and property panel registry.

## Dependency constraints

- Depends on `@xflow/workflow-core`.
- Declares `react` and `react-dom` as peer dependencies.
- No Umi / Ant Design / ProLayout dependencies.
