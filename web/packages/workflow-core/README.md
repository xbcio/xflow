# @xflow/workflow-core

Workflow DSL types, YAML/JSON utilities, DAG traversal and spec migration.

## Dependency constraints

- No React / React DOM.
- No `@xyflow/react`, `antd`, `@umijs/max`, `@ant-design/pro-layout`.
- No HTTP client / API calls.
